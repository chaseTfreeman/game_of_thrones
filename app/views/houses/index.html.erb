
<div class="whitespace"></div>
<div id="houses" class="transitions-enabled">
  <% @houses.each do |house| %>
  <div class="box">
    <%= link_to image_tag("#{house.img_url}"), house_path(house) %>
  </div>
    <% end %>
</div>
